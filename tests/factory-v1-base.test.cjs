// ════════════════════════════════════════════════════════════════════════════
// DEPRECATED — Factory CRM Base v1.5 round-trip tests
// ════════════════════════════════════════════════════════════════════════════
//
// The v1.0/v1.5 factory architecture under js/modules/factory/ was retired on
// 2026-05-02 (see improvement-backlog.md → Factory Architecture Migration Plan).
// All entities tested by the original suite (FactoryJob, FactoryFrame,
// FactoryStockMovement, FactoryTaskQueue, FactoryStuckJobMonitor,
// FactoryMaterialOrder, FactoryGoodsReceipt, FactoryAuditChain, etc.) no
// longer exist in the codebase. The legacy `modules/16*, 40-50` architecture
// is the canonical factory layer going forward.
//
// This file is left as a stub to preserve the file path in git history. It
// can be safely deleted at any time. New factory tests should target the
// legacy modules directly.

'use strict';

console.log('[factory-v1-base.test.cjs] Skipped — v1 architecture retired 2026-05-02.');
process.exit(0);
